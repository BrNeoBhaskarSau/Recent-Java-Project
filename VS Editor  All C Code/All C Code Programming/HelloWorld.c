#include <stdio.h>
int main() {
   // printf() displays the string inside quotation
   printf(" first enter the gcc commend and then type file name then hit the enter\n then type ./a.exe it is ineouf the running the program..");
   printf("Hello\nBhaskar../");
   printf("this my first running c //");
   return 0;
}